# Public release repository policy

Only CI may publish release packages. Before publication, CI must verify the manifest signature and the SHA-256 hash of every downloadable file.

Never commit credentials, internal service addresses, private keys, user data, source archives, or local build logs.
